// Global type declarations for browser-specific APIs

interface Navigator {
  standalone?: boolean; // iOS Safari PWA indicator
}

interface Window {
  // Add any window-specific properties here if needed
} 