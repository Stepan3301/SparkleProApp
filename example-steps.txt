<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SparklePro - Cleaning Order Steps</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
            color: #333;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 25px;
            padding: 40px;
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.15);
        }

        .title {
            text-align: center;
            font-size: 28px;
            font-weight: 700;
            color: #2E86AB;
            margin-bottom: 40px;
        }

        /* Steps Progress */
        .steps-container {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 50px;
            position: relative;
            padding: 20px 0;
        }

        .step-cloud {
            position: relative;
            width: 80px;
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            font-weight: 700;
            color: white;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: 2;
        }

        /* Cloud shape using clip-path */
        .step-cloud::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #adb5bd 0%, #6c757d 100%);
            transition: all 0.4s ease;
            clip-path: polygon(
                25% 0%, 
                75% 0%, 
                100% 25%, 
                100% 75%, 
                75% 100%, 
                25% 100%, 
                0% 75%, 
                0% 25%
            );
            border-radius: 50% 20% 50% 20%;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        /* Cloud decorative bubbles */
        .step-cloud::after {
            content: '';
            position: absolute;
            top: -8px;
            right: -8px;
            width: 20px;
            height: 20px;
            background: inherit;
            border-radius: 50%;
            opacity: 0.7;
            transition: all 0.4s ease;
        }

        /* Current step - blue gradient */
        .step-cloud.current::before {
            background: linear-gradient(135deg, #2E86AB 0%, #4A90E2 100%);
            transform: scale(1.1);
            box-shadow: 0 8px 25px rgba(46, 134, 171, 0.4);
            animation: pulse-current 2s infinite;
        }

        .step-cloud.current {
            transform: scale(1.05);
        }

        /* Completed step - green gradient */
        .step-cloud.completed::before {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            box-shadow: 0 6px 20px rgba(40, 167, 69, 0.3);
        }

        .step-cloud.completed::after {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
        }

        /* Pending step - gray gradient */
        .step-cloud.pending::before {
            background: linear-gradient(135deg, #e9ecef 0%, #adb5bd 100%);
            box-shadow: 0 4px 15px rgba(173, 181, 189, 0.2);
        }

        .step-cloud.pending {
            color: #6c757d;
        }

        /* Step number */
        .step-number {
            position: relative;
            z-index: 3;
            font-weight: 800;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
        }

        /* Connecting lines between clouds */
        .step-connector {
            flex: 1;
            height: 4px;
            background: linear-gradient(90deg, #e9ecef 0%, #adb5bd 50%, #e9ecef 100%);
            margin: 0 10px;
            border-radius: 2px;
            position: relative;
            overflow: hidden;
        }

        .step-connector.active {
            background: linear-gradient(90deg, #28a745 0%, #20c997 50%, #28a745 100%);
            box-shadow: 0 2px 8px rgba(40, 167, 69, 0.3);
        }

        .step-connector.active::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
            animation: shimmer 2s infinite;
        }

        /* Step labels */
        .step-labels {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
            padding: 0 40px;
        }

        .step-label {
            font-size: 12px;
            font-weight: 600;
            color: #6c757d;
            text-align: center;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: color 0.3s ease;
        }

        .step-label.active {
            color: #2E86AB;
        }

        .step-label.completed {
            color: #28a745;
        }

        /* Form content */
        .form-content {
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.05);
            border: 2px solid rgba(46, 134, 171, 0.1);
        }

        .step-content {
            display: none;
            animation: fadeIn 0.5s ease;
        }

        .step-content.active {
            display: block;
        }

        .step-title {
            font-size: 20px;
            font-weight: 700;
            color: #2E86AB;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Checkbox styling */
        .checkbox-group {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-bottom: 30px;
        }

        .checkbox-item {
            display: flex;
            align-items: center;
            padding: 15px;
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            border: 2px solid #e9ecef;
            border-radius: 15px;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .checkbox-item:hover {
            border-color: #2E86AB;
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(46, 134, 171, 0.1);
        }

        .checkbox-item input[type="checkbox"] {
            width: 20px;
            height: 20px;
            margin-right: 15px;
            accent-color: #2E86AB;
            cursor: pointer;
        }

        .checkbox-label {
            font-size: 16px;
            font-weight: 500;
            color: #495057;
            cursor: pointer;
        }

        /* Next button */
        .next-button {
            width: 100%;
            padding: 16px 32px;
            background: linear-gradient(135deg, #2E86AB 0%, #A23B72 100%);
            color: white;
            border: none;
            font-family: inherit;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            clip-path: polygon(
                8% 0%, 
                92% 0%, 
                100% 25%, 
                100% 75%, 
                92% 100%, 
                8% 100%, 
                0% 75%, 
                0% 25%
            );
        }

        .next-button:hover {
            background: linear-gradient(135deg, #1e5f7a 0%, #8b2f5e 100%);
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(46, 134, 171, 0.4);
        }

        .next-button:active {
            transform: translateY(-1px);
        }

        .next-button:disabled {
            background: linear-gradient(135deg, #6c757d 0%, #adb5bd 100%);
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        /* Animations */
        @keyframes pulse-current {
            0%, 100% { 
                box-shadow: 0 8px 25px rgba(46, 134, 171, 0.4);
            }
            50% { 
                box-shadow: 0 12px 35px rgba(46, 134, 171, 0.6);
            }
        }

        @keyframes shimmer {
            0% { left: -100%; }
            100% { left: 100%; }
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Floating particles */
        .particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: rgba(46, 134, 171, 0.3);
            border-radius: 50%;
            animation: float 6s infinite ease-in-out;
        }

        .particle:nth-child(1) { left: 10%; animation-delay: 0s; }
        .particle:nth-child(2) { left: 20%; animation-delay: 1s; }
        .particle:nth-child(3) { left: 30%; animation-delay: 2s; }
        .particle:nth-child(4) { left: 40%; animation-delay: 3s; }
        .particle:nth-child(5) { left: 50%; animation-delay: 4s; }

        @keyframes float {
            0%, 100% { 
                transform: translateY(100vh) rotate(0deg); 
                opacity: 0; 
            }
            10%, 90% { 
                opacity: 1; 
            }
            50% { 
                transform: translateY(-10px) rotate(180deg); 
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .container {
                padding: 20px;
                margin: 10px;
            }
            
            .steps-container {
                padding: 10px 0;
            }
            
            .step-cloud {
                width: 60px;
                height: 60px;
                font-size: 18px;
            }
            
            .step-labels {
                padding: 0 20px;
            }
            
            .step-label {
                font-size: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="particles">
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
    </div>

    <div class="container">
        <h1 class="title">🧽 SparklePro Cleaning Order</h1>
        
        <!-- Steps Progress -->
        <div class="steps-container">
            <div class="step-cloud current" data-step="1">
                <span class="step-number">1</span>
            </div>
            <div class="step-connector"></div>
            <div class="step-cloud pending" data-step="2">
                <span class="step-number">2</span>
            </div>
            <div class="step-connector"></div>
            <div class="step-cloud pending" data-step="3">
                <span class="step-number">3</span>
            </div>
            <div class="step-connector"></div>
            <div class="step-cloud pending" data-step="4">
                <span class="step-number">4</span>
            </div>
        </div>

        <div class="step-labels">
            <div class="step-label active">Service Type</div>
            <div class="step-label">Property Details</div>
            <div class="step-label">Schedule</div>
            <div class="step-label">Confirmation</div>
        </div>

        <!-- Form Content -->
        <div class="form-content">
            <!-- Step 1: Service Type -->
            <div class="step-content active" data-step="1">
                <h2 class="step-title">Choose Your Cleaning Service</h2>
                <div class="checkbox-group">
                    <label class="checkbox-item">
                        <input type="checkbox" name="service" value="regular">
                        <span class="checkbox-label">🏠 Regular House Cleaning</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="service" value="deep">
                        <span class="checkbox-label">✨ Deep Cleaning Service</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="service" value="office">
                        <span class="checkbox-label">🏢 Office Cleaning</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="service" value="move">
                        <span class="checkbox-label">📦 Move-in/Move-out Cleaning</span>
                    </label>
                </div>
            </div>

            <!-- Step 2: Property Details -->
            <div class="step-content" data-step="2">
                <h2 class="step-title">Property Information</h2>
                <div class="checkbox-group">
                    <label class="checkbox-item">
                        <input type="checkbox" name="rooms" value="1-2">
                        <span class="checkbox-label">🏠 1-2 Bedrooms</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="rooms" value="3-4">
                        <span class="checkbox-label">🏡 3-4 Bedrooms</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="rooms" value="5+">
                        <span class="checkbox-label">🏰 5+ Bedrooms</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="extras" value="pets">
                        <span class="checkbox-label">🐕 Pet-Friendly Cleaning</span>
                    </label>
                </div>
            </div>

            <!-- Step 3: Schedule -->
            <div class="step-content" data-step="3">
                <h2 class="step-title">Schedule Your Cleaning</h2>
                <div class="checkbox-group">
                    <label class="checkbox-item">
                        <input type="checkbox" name="frequency" value="weekly">
                        <span class="checkbox-label">📅 Weekly Cleaning</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="frequency" value="biweekly">
                        <span class="checkbox-label">📆 Bi-weekly Cleaning</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="frequency" value="monthly">
                        <span class="checkbox-label">🗓️ Monthly Cleaning</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="frequency" value="onetime">
                        <span class="checkbox-label">⚡ One-time Service</span>
                    </label>
                </div>
            </div>

            <!-- Step 4: Confirmation -->
            <div class="step-content" data-step="4">
                <h2 class="step-title">Confirm Your Order</h2>
                <div class="checkbox-group">
                    <label class="checkbox-item">
                        <input type="checkbox" name="confirm" value="terms">
                        <span class="checkbox-label">✅ I agree to Terms & Conditions</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="confirm" value="contact">
                        <span class="checkbox-label">📞 Contact me for confirmation</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="confirm" value="newsletter">
                        <span class="checkbox-label">📧 Subscribe to cleaning tips newsletter</span>
                    </label>
                    <label class="checkbox-item">
                        <input type="checkbox" name="confirm" value="ready">
                        <span class="checkbox-label">🎉 I'm ready to book this service!</span>
                    </label>
                </div>
            </div>
        </div>

        <button class="next-button" id="nextBtn">Next Step</button>
    </div>

    <script>
        class CleaningOrderSteps {
            constructor() {
                this.currentStep = 1;
                this.totalSteps = 4;
                this.init();
            }

            init() {
                this.bindEvents();
                this.updateUI();
            }

            bindEvents() {
                document.getElementById('nextBtn').addEventListener('click', () => this.nextStep());
                
                // Add click handlers for step clouds
                document.querySelectorAll('.step-cloud').forEach(cloud => {
                    cloud.addEventListener('click', (e) => {
                        const step = parseInt(e.currentTarget.dataset.step);
                        if (step <= this.currentStep) {
                            this.goToStep(step);
                        }
                    });
                });
            }

            nextStep() {
                if (this.currentStep < this.totalSteps) {
                    this.currentStep++;
                    this.updateUI();
                } else {
                    this.completeOrder();
                }
            }

            goToStep(step) {
                this.currentStep = step;
                this.updateUI();
            }

            updateUI() {
                // Update step clouds
                document.querySelectorAll('.step-cloud').forEach((cloud, index) => {
                    const stepNumber = index + 1;
                    cloud.classList.remove('current', 'completed', 'pending');
                    
                    if (stepNumber < this.currentStep) {
                        cloud.classList.add('completed');
                    } else if (stepNumber === this.currentStep) {
                        cloud.classList.add('current');
                    } else {
                        cloud.classList.add('pending');
                    }
                });

                // Update connectors
                document.querySelectorAll('.step-connector').forEach((connector, index) => {
                    if (index < this.currentStep - 1) {
                        connector.classList.add('active');
                    } else {
                        connector.classList.remove('active');
                    }
                });

                // Update step labels
                document.querySelectorAll('.step-label').forEach((label, index) => {
                    const stepNumber = index + 1;
                    label.classList.remove('active', 'completed');
                    
                    if (stepNumber < this.currentStep) {
                        label.classList.add('completed');
                    } else if (stepNumber === this.currentStep) {
                        label.classList.add('active');
                    }
                });

                // Update step content
                document.querySelectorAll('.step-content').forEach(content => {
                    content.classList.remove('active');
                });
                document.querySelector(`[data-step="${this.currentStep}"]`).classList.add('active');

                // Update button text
                const nextBtn = document.getElementById('nextBtn');
                if (this.currentStep === this.totalSteps) {
                    nextBtn.textContent = 'Complete Order 🎉';
                } else {
                    nextBtn.textContent = 'Next Step';
                }
            }

            completeOrder() {
                // Add completion animation
                document.querySelector('.container').style.transform = 'scale(1.02)';
                document.querySelector('.container').style.transition = 'transform 0.3s ease';
                
                setTimeout(() => {
                    alert('🎉 Order completed successfully! We will contact you soon.');
                    document.querySelector('.container').style.transform = 'scale(1)';
                }, 300);
            }
        }

        // Initialize the steps system
        document.addEventListener('DOMContentLoaded', () => {
            new CleaningOrderSteps();
        });

        // Add entrance animation
        window.addEventListener('load', () => {
            const container = document.querySelector('.container');
            container.style.opacity = '0';
            container.style.transform = 'translateY(30px) scale(0.95)';
            
            setTimeout(() => {
                container.style.transition = 'all 0.6s cubic-bezier(0.4, 0, 0.2, 1)';
                container.style.opacity = '1';
                container.style.transform = 'translateY(0) scale(1)';
            }, 100);
        });
    </script>
</body>
</html>